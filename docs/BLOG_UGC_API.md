# 📝 Blog UGC API - Документация

Пользовательский блог для СПАРКОМ с модерацией, антиспамом и правами доступа.

**API URL**: `https://functions.poehali.dev/756eea00-7132-49ee-a076-e43afac6d5a6`

---

## 🎯 Основные возможности

✅ **CRUD пользовательских постов**  
✅ **Система статусов**: draft, published, hidden, blocked  
✅ **Видимость**: public, followers, private  
✅ **Антиспам**: лимит 10 постов в сутки  
✅ **Автопубликация** для мастеров и организаторов  
✅ **Модерация** администраторами  
✅ **Привязка к событиям и баням**  
✅ **Slug генерация** с транслитерацией  
✅ **Счетчик просмотров**  

---

## 📊 API Endpoints

### 1. Получение списка постов

```http
GET /
```

**Query параметры:**
- `author` - ID автора
- `status` - Статус (только для админов)
- `related_event` - ID связанного события
- `date_from` - Дата от (YYYY-MM-DD)
- `date_to` - Дата до (YYYY-MM-DD)
- `limit` - Лимит (по умолчанию 20, макс 100)
- `offset` - Смещение

**Пример:**
```javascript
const response = await fetch('https://functions.poehali.dev/756eea00-7132-49ee-a076-e43afac6d5a6/');
const data = await response.json();
console.log(data.posts); // Массив постов
```

**Ответ:**
```json
{
  "posts": [
    {
      "id": 1,
      "slug": "moj-opyt-pareniya",
      "title": "Мой опыт парения",
      "excerpt": "Краткое описание...",
      "content": "Полный текст...",
      "author": "Иван Иванов",
      "author_id": 5,
      "author_avatar": "https://...",
      "status": "published",
      "visibility": "public",
      "views_count": 42,
      "created_at": "2026-01-15T10:00:00",
      "published_at": "2026-01-15T10:05:00"
    }
  ],
  "total": 150,
  "limit": 20,
  "offset": 0
}
```

---

### 2. Получение конкретного поста

```http
GET /?slug={slug}
```

**Пример:**
```javascript
const response = await fetch('https://functions.poehali.dev/756eea00-7132-49ee-a076-e43afac6d5a6/?slug=moj-opyt-pareniya');
const post = await response.json();
```

**Ответ:** Объект поста с увеличенным счетчиком просмотров.

**Ошибки:**
- `404` - Пост не найден
- `403` - Доступ запрещен (для private/followers постов)

---

### 3. Создание поста

```http
POST /
Authorization: Bearer {access_token}
```

**Тело запроса:**
```json
{
  "title": "Заголовок поста",
  "content": "Содержимое поста (минимум 50 символов)",
  "visibility": "public",
  "related_event_id": 123
}
```

**Поля:**
- `title` *(обязательно)* - Заголовок (мин 5 символов)
- `content` *(обязательно)* - Содержимое (мин 50 символов)
- `visibility` - Видимость: `public` (по умолчанию), `followers`, `private`
- `related_event_id` - ID связанного события (опционально)

**Пример:**
```javascript
const token = localStorage.getItem('access_token');
const response = await fetch('https://functions.poehali.dev/756eea00-7132-49ee-a076-e43afac6d5a6/', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`
  },
  body: JSON.stringify({
    title: 'Мой опыт парения',
    content: 'Сегодня я посетил баню и хочу поделиться своими впечатлениями...',
    visibility: 'public'
  })
});

const result = await response.json();
console.log(result.slug); // Slug созданного поста
```

**Ответ:**
```json
{
  "id": 42,
  "slug": "moj-opyt-pareniya",
  "status": "published",
  "created_at": "2026-01-16T12:00:00"
}
```

**Автопубликация:**
- Роли `master`, `organizer`, `admin` → status = `published`
- Остальные роли → status = `draft`

**Ошибки:**
- `401` - Требуется авторизация
- `429` - Превышен лимит (10 постов в сутки)
- `400` - Валидация не прошла

---

### 4. Обновление поста

```http
PUT /?slug={slug}
Authorization: Bearer {access_token}
```

**Тело запроса:**
```json
{
  "title": "Новый заголовок",
  "content": "Обновленное содержимое",
  "visibility": "public"
}
```

**Права:**
- Автор поста может редактировать
- Администратор может редактировать любые посты и менять `status`

**Пример:**
```javascript
const token = localStorage.getItem('access_token');
const response = await fetch('https://functions.poehali.dev/756eea00-7132-49ee-a076-e43afac6d5a6/?slug=moj-opyt-pareniya', {
  method: 'PUT',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`
  },
  body: JSON.stringify({
    title: 'Обновленный заголовок',
    content: 'Новое содержимое поста...'
  })
});
```

**Ответ:**
```json
{
  "id": 42,
  "slug": "moj-opyt-pareniya",
  "status": "published"
}
```

**Ошибки:**
- `401` - Требуется авторизация
- `403` - Нет прав на редактирование
- `404` - Пост не найден

---

### 5. Модерация поста (только администраторы)

```http
POST /
Authorization: Bearer {admin_token}
```

**Тело запроса:**
```json
{
  "action": "moderate",
  "post_id": 42,
  "action": "block",
  "reason": "Спам"
}
```

**Действия:**
- `publish` - Опубликовать пост
- `hide` - Скрыть пост
- `block` - Заблокировать пост

**Пример:**
```javascript
const token = localStorage.getItem('access_token');
const response = await fetch('https://functions.poehali.dev/756eea00-7132-49ee-a076-e43afac6d5a6/', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`
  },
  body: JSON.stringify({
    action: 'moderate',
    post_id: 42,
    action: 'block',
    reason: 'Нарушение правил сообщества'
  })
});
```

**Ответ:**
```json
{
  "status": "success",
  "new_status": "blocked"
}
```

**Ошибки:**
- `403` - Недостаточно прав (не администратор)
- `404` - Пост не найден

---

## 🔒 Авторизация

API использует токен из системы Auth:

```javascript
// Получение токена при входе
const loginResponse = await fetch('AUTH_API_URL/', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ email, password })
});

const { access_token } = await loginResponse.json();
localStorage.setItem('access_token', access_token);

// Использование токена в Blog API
const headers = {
  'Authorization': `Bearer ${access_token}`
};
```

---

## 🛡️ Антиспам и безопасность

### Ограничения:
- ✅ Максимум **10 постов в сутки** на пользователя
- ✅ Минимум **5 символов** в заголовке
- ✅ Минимум **50 символов** в содержимом
- ✅ Автоматическая транслитерация slug
- ✅ Защита от XSS (на фронтенде)

### Модерация:
- Все действия модераторов логируются в `blog_moderation_log`
- История изменений статуса поста
- Причины блокировки

---

## 📋 Статусы постов

| Статус | Описание | Видимость |
|--------|----------|-----------|
| `draft` | Черновик | Только автор |
| `published` | Опубликован | Согласно visibility |
| `hidden` | Скрыт | Только автор и админ |
| `blocked` | Заблокирован | Только админ |

---

## 👁️ Уровни видимости

| Visibility | Кто видит |
|------------|-----------|
| `public` | Все пользователи |
| `followers` | Подписчики автора (закладка на будущее) |
| `private` | Только автор |

---

## 🏗️ Структура базы данных

### Таблицы:
- `blog_posts` - Посты блога
- `blog_moderation_log` - Логи модерации
- `blog_rate_limits` - Антиспам лимиты
- `blog_categories_v2` - Категории
- `blog_reports` - Жалобы (закладка)

### Категории:
1. Банная культура
2. Здоровье
3. Мастера
4. События
5. Рецепты
6. Обзоры

---

## ✅ Критерии приёмки (из ТЗ)

- ✅ Пользователь может создать, сохранить и опубликовать пост
- ✅ Работает черновик (status = draft)
- ✅ Права доступа соблюдаются
- ✅ Администратор может модерировать
- ✅ Посты отображаются в ленте с фильтрами
- ✅ Антиспам-ограничения работают (10 постов/сутки)
- ✅ API задокументировано

---

## 🚀 Следующие шаги

1. **Фронтенд страницы:**
   - `/blog` - Лента блога
   - `/blog/{username}` - Блог пользователя
   - `/blog/post/{slug}` - Просмотр поста
   - `/profile/me/posts` - Мои посты

2. **Редактор:**
   - WYSIWYG или Markdown
   - Autosave черновиков
   - Preview перед публикацией

3. **Дополнительные функции:**
   - Комментарии
   - Лайки
   - Подписки
   - Рекомендации

**Статус**: 🟢 Backend UGC Blog готов на 100%
